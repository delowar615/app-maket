[package]
name = "app-maket"
version = "0.1.0"
edition = "2024"

[dependencies]
# Добавляем фичу image, иначе Rust не увидит этот виджет
iced = { version = "0.14", features = ["image", "wgpu", "tiny-skia"] }

